# 掌机风格贪吃蛇

## 直接运行

```bash
./run.sh
```

启动后会自动打开浏览器（如果当前环境支持），也可手动访问：

- `http://127.0.0.1:4173/index.html`

## 可选参数

```bash
./run.sh --host 0.0.0.0 --port 8080 --no-open
```

- `--host`：监听地址
- `--port`：监听端口
- `--no-open`：只启动服务，不尝试自动打开浏览器
